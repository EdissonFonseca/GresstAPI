public enum PartyRole
{
    Customer,       // genera residuos, solicita servicios
    Supplier,       // ofrece servicios de gestión
    Hauler,         // transporta residuos
    Operator,       // ejecuta operaciones (puede ser el mismo sistema)
    Regulator,      // ente de control
    Owner           // la propia empresa (tu empresa)
}
