public class TransferWasteHandler
{
    public async Task Handle(TransferWasteCommand cmd)
    {
        //var waste = await _repo.Get(cmd.WasteId);

        //var operation = WasteOperationFactory.CreateTransfer(
        //    cmd.FromOwner,
        //    cmd.ToOwner,
        //    cmd.ProcessId);

        //waste.ApplyOperation(operation);

        //await _repo.Save(waste);
    }
}